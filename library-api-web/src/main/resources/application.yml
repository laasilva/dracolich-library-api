spring:
  application:
    name: dracolich-library-api
  webflux:
    base-path: dracolich-library/api/v0/
  data:
    mongodb:
      auto-index-creation: true
  mongodb:
    database: ${MONGODB_DATABASE:dracolich-library-db}
    uri: ${MONGODB_URI:mongodb://localhost:27017/dracolich-library-db}
    representation:
      uuid: standard

server:
  port: ${PORT:8080}

cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:dracolich}
  api-key: ${CLOUDINARY_API_KEY:xxxxxxxxxxxxx}
  api-secret: ${CLOUDINARY_API_SECRET:xxxxxxxxxxxxxx}

logging:
  level:
    dm.dracolich: DEBUG
    org.springframework.data.mongodb: INFO
